// OAuth configuration for Google Drive integration.
// NOTE: Do not check secrets into version control for production.
// This file provides a public `clientId` for the installed app flow (no secret required).

const String googleDriveClientId = '613700464106-rk43p5d7dichbg972kat9ulvf3e3gt73.apps.googleusercontent.com';

// If you need to use an OAuth client secret (web/installed flows), set it via local environment
// or a secure config mechanism and avoid committing it.
const String googleDriveClientSecret = ''; // keep empty for installed/desktop flows
